<% @articles.each do |article| %>

<ul><%= link_to article.title, article_path(article), class: 'article_title' %></ul>

<% end %>

<%= link_to "Create a New Article", new_article_path, class: 'new_article' %>